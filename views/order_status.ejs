<!DOCTYPE html>
<html>
<head><title>Order Status</title></head>
<body>
<h1>Order Status</h1>
<ul>
  <% orders.forEach(order => { %>
    <li>
      <a href="/orders/<%= order._id %>">Order <%= order._id %></a> -
      Status: <%= order.status %>
      <% if (isHost) { %>
        (Customer: <%= order.user %>)
      <% } %>
    </li>
  <% }) %>
</ul>
<a href="/products">Back to Products</a>
</body>
</html>
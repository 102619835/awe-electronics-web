<!DOCTYPE html>
<html>
<head>
  <title><%= title || "AWE Electronics" %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
    <div class="container">
      <a class="navbar-brand" href="/products">AWE Electronics</a>
      <div>
        <% if (role === 'host') { %>
          <a class="btn btn-light" href="/products/new">Add Product</a>
          <a class="btn btn-light" href="/orders/host/all">All Orders</a>
        <% } else if (role === 'customer') { %>
          <a class="btn btn-light" href="/cart">Cart</a>
          <a class="btn btn-light" href="/orders">My Orders</a>
        <% } %>
        <% if (currentUser) { %>
          <a class="btn btn-outline-light" href="/logout">Logout</a>
        <% } else { %>
          <a class="btn btn-outline-light" href="/login">Login</a>
        <% } %>
      </div>
    </div>
  </nav>
  <div class="container">
    <%- body %>
  </div>
</body>
</html>